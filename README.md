dotfiles
========

```
git clone https://github.com/johnny-miyake/dotfiles.git
cd dotfiles
git submodule init
git submodule update
cd
ln -s dotfiles/_vim .vim
ln -s dotfiles/_vimrc .vimrc
ln -s dotfiles/_zshrc .zshrc
```
